<%
  meta title: "#{t('works_title')}",
       description: "#{t('work_description')}",
       og: {
        title: "#{t('works_title')}",
        description: "#{t('work_description')}"
       }
%>

<section id="portfolio" class="portfolio_page">
  <div class="container">
    <div class="col-sm-12 text-center">
      <h1><%= t('works_heading') %></h1>
    </div>

    <div class="col-sm-12">
      <% @works.each_slice(2) do |slice| -%>
        <div class="row small_top_margin">
          <% slice.each do |work| -%>
            <div class="col-sm-6">
              <div class="col-sm-12">

                <%= link_to work.work_image.url(:original), class: "folio-detail fancybox", rel: "lightbox" do %>

                  <div id="isotope_container" class="isotope text-center">
                    <div class="btn-group">
                      <div class="isotope-item gallery-item brand">
                        <div class="gallery-image portfolio-content">
                          <%= image_tag work.work_image(:medium), class: "img-responsive work_image" %>
                          <div class="overlay text-center">
                            <i class="fa fa-search-plus folio-detail"></i>
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>

                <% end %>

              </div>

              <div class="col-sm-12 regular_text work_title">
                <strong><%= work.title %></strong>
              </div>
              <div class="col-sm-12 regular_text work_department">
                <%= work.department %>
              </div>
              <div class="col-sm-12 regular_text work_description">
                <%= work.description %>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</section>
